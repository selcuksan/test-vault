kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: test
  namespace: altyapi
spec:
  path: /
  to:
    kind: Service
    name: argocd-sample-server
    weight: 100
  port:
    targetPort: 8083
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Allow

